  	<%= render 'books/list', books: @books, book_new: @book_new ,user: @user %>

  				<!-- ここから右側本体 -->
  				<div class='col-xs-9'>
  					<h2>Book detail</h2>
  					<table class='table'>
  						<tr>
  							<td>image<%= @book.user.name %></td>
  							<td><%= @book.title%></td>
  							<td><%= @book.body%>
  								<% if @book.user == current_user %>
  								<%= link_to "Edit", edit_book_path(@book) %>
  								<%= link_to "Destroy", book_path(@book), method: :delete %>
  								<% end %>
  							</td>
  						</tr>
  					</table>
  				</div>
  			</div>
  		</div>
  	</div>


